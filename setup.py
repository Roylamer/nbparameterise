from distutils.core import setup
setup(
  name = 'nbparameterise',
  packages = ['nbparameterise'], # this must be the same as the name above
  version = '0.0.1',
  description = 'Execute notebook with no timeout',
  author = 'Icarus So',
  author_email = 'icarus.so@gmail.com',
  url = 'https://github.com/IcarusSO/nbparameterise', # use the URL to the github repo
  keywords = [], # arbitrary keywords
  classifiers = [],
  install_requires=[
  ]
)
